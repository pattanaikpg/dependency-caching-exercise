{
  "name": "service-containers-exercise",
  "version": "1.0.0",
  "description": "Integration test exercise with PostgreSQL and Redis service containers",
  "scripts": {
    "test": "jest --forceExit --detectOpenHandles",
    "test:integration": "jest --forceExit --detectOpenHandles --testPathPattern=app.test"
  },
  "dependencies": {
    "ioredis": "^5.3.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
